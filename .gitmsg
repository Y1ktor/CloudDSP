feat: finalize EQ visualizer UI layout and local playback

- Add local file upload support using Blob URLs for instant, zero-latency audio preview.
- Implement Highpass and Lowpass filter bands (Bands 0 and 4) to act as frequency cut-offs.
- Expand canvas layout: increase height to 400px, add 60px top padding, and draw a horizontal ceiling grid line to perfectly frame the spectrum analyzer.
- Update hit-detection math and visual routing to safely ignore un-gained cut filters and respect dynamic layout padding.
