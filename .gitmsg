feat: implement fully interactive 2D EQ canvas controls

- Upgrade EQ to 3 fully configurable Peaking (Bell) bands (Freq, Q, Gain).
- Implement 2D canvas hit-detection, scaling physical mouse coordinates to internal resolution.
- Add inverse logarithmic math (`getFreqFromX`) to translate horizontal pixel movement back to precise frequency values.
- Render draggable control nodes on the EQ peaks with dynamic hover and active visual states.
- Sync bi-directional state between canvas dragging, Web Audio API filters, and HTML input sliders in real-time.
- Layer individual faint filter curves beneath the cumulative white polynomial curve for enhanced visual feedback.