chore: restructure DSP Lambda files and track packaged template

- Restructure `src/DSP/` into isolated `src`, `tests`, and `assets` directories for clean and lightweight lambda packaging.
- Add mock SQS payload `message.json` to `assets/` for testing bitcrusher.
- Fix relative `Code` path in `upload-pipeline.yaml` to `../src/DSP/src`.
- Add generated deployment artifact `packaged.yaml`.
