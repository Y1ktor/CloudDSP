feat: implement interactive DSP worker lambda and test suite

- Refactor `dsp.py` into an SQS-triggered AWS Lambda handler that manages S3 downloads/uploads and ephemeral storage.
- Implement Ring Modulation effect using native numpy math.
- Create standalone test scripts (`test_bitcrush.py`, `test_flanger.py`, `test_ring_mod.py`) for isolated DSP development.
- Update `upload-pipeline.yaml` with the DSP Worker Lambda, assigning 2GB memory/ephemeral storage, explicit IAM bucket permissions, and setting SQS BatchSize to 1 to prevent processing timeouts.
- Update `gemini.md` architecture logic to document the API-driven interactive pipeline.
